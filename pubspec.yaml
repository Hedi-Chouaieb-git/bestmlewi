name: supabase_app
description: Flutter app with Supabase connection

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter_map: ^8.2.2
  http: ^1.6.0
  latlong2: ^0.9.1
  flutter:
    sdk: flutter
  supabase_flutter: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/